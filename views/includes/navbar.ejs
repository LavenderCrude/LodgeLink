<nav
  class="navbar navbar-expand-lg navbar-light bg-white shadow-sm sticky-top border-bottom"
  style="z-index: 1030"
>
  <div class="container-fluid px-4">
    <a class="navbar-brand fw-bold text-danger" href="/listing">
      <i class="fa-solid fa-hotel me-2"></i>
    </a>

    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 gap-3">
        <li class="nav-item">
          <a class="nav-link fw-semibold" href="/listing">Explore</a>
        </li>
        <li class="nav-item">
          <a class="nav-link fw-semibold" href="/listing/newLodge"
            >Add New Listing</a
          >
        </li>
      </ul>
      <!-- Search Bar -->
      <form
        class="d-flex align-items-center gap-2 mx-auto my-2"
        method="GET"
        action="/listing"
        style="max-width: 500px; width: 100%"
      >
        <input
          class="form-control rounded-start"
          type="search"
          placeholder="Search by title, location, or price..."
          name="q"
          value="<%= typeof q !== 'undefined' ? q : '' %>"
        />
        <button class="btn btn-white rounded-end px-3" type="submit">
          <i class="fa-solid fa-magnifying-glass text-red"></i>
        </button>
      </form>

      <!-- Right Side Auth -->
      <ul class="navbar-nav ms-auto d-flex align-items-center gap-3">
        <% if (!currUser) { %>
        <li class="nav-item">
          <a class="nav-link fw-semibold" href="/signup">Sign Up</a>
        </li>
        <li class="nav-item">
          <a class="nav-link fw-semibold" href="/login">Login</a>
        </li>
        <% } else { %>
        <li class="nav-item">
          <span class="navbar-text fw-semibold">
            Hi, <%= currUser.username %>
          </span>
        </li>
        <li class="nav-item">
          <a class="nav-link fw-semibold text-danger" href="/logout">Logout</a>
        </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
